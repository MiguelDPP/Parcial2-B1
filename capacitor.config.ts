import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.softcode.upc',
  appName: 'Restaurante-UPC',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
